<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Пошук</title>
    <link rel="stylesheet" href="/bootstrap/css/bootstrap.min.css">
</head>
<body>
    <%- include('partials/navbar') %>

    <div class="container mt-4">
        <h2>Пошук друзів</h2>
        <p>Список знайдених користувачів:</p>
        <div class="row">
            <% if (users && users.length > 0) { %>
                <% users.forEach(function(usr) { %>
                    <div class="col-md-4 mb-3">
                        <div class="card">
                            <div class="d-flex align-items-center">
                                <img src="<%= usr.image %>" class="rounded-circle me-3" width="100" height="100" alt="Аватар">
                                <div class="card-body">
                                    <h5 class="card-title">
                                        <a href="/profile/<%= usr.id %>" style="font-weight: bold; color: black; text-decoration: none;">
                                            <%= usr.name %>
                                        </a>
                                    </h5>
                                    <p class="card-text"><%= usr.email %></p>
                                </div>
                            </div>
                        </div>
                    </div>
                <% }); %>
            <% } else { %>
                <p>Користувачів не знайдено.</p>
            <% } %>
        </div>  
    </div>
    
    

    <script src="/bootstrap/js/bootstrap.bundle.min.js"></script>
</body>
</html>
